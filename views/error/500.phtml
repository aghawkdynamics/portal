<?php

use App\Core\Config;
use App\Core\Debug;
use App\Model\Account\User;

  $exception = $this->var('exception');
?>
<style>
    .not-found-container {
        max-width: 500px;
        margin: 60px auto;
        padding: 40px 30px;
        background: #fff;
        border-radius: 10px;
        box-shadow: 0 2px 16px rgba(0,0,0,0.07);
        text-align: center;
    }
    .not-found-container h1 {
        font-size: 4em;
        margin-bottom: 0.2em;
        color: #e74c3c;
    }
    .not-found-container p {
        font-size: 1.2em;
        color: #555;
        margin-bottom: 1.5em;
    }
    
    
</style>
<div class="not-found-container">
    <h1>500</h1>
    <p>Sorry, an internal server error occurred.</p>
    <?php if (User::isAdmin() || Config::get('debug')): ?>
        <p><strong>Error Details:</strong></p>
        <p><?= $exception->getMessage() ?></p>
        <pre style="text-align: left; background: #f8f8f8; padding: 10px; border-radius: 5px; font-size: 0.9em; color: #333;">
<?= $exception->getTraceAsString() ?>
        </pre>
    <?php endif; ?>
    <p>We are working to fix this issue. Please try again later.</p>
    <p>If the problem persists, please contact support.</p>
    
    <a href="/">Go to Home Page</a>
</div>